{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "serve": "nodemon app.js && open http://localhost:5000"
  },
  "dependencies": {
    "bootstrap": "^4.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
